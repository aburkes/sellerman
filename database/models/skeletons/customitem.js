/*
Basic structure that should be used for custom items. Use this object and extend
it for new types of custom items.

On second thought, it really shouldn't be needed.


let Sequelize = require("sequelize");

module.exports = {
    options: Sequelize.INTEGER
};

*/